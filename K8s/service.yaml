apiVersion: v1
kind: Service
metadata:
  name: nocap-fastapi
  namespace: nocap
  labels:
    app: nocap-fastapi
spec:
  type: ClusterIP
  selector:
    app: nocap-fastapi
  ports:
    - name: http
      port: 80
      targetPort: 80
      appProtocol: http
